# Signed Grok webhook — never commit. Mode 0600.
# Helsinki pushes facts; the LLM does not poll and does not call the broker.

GROK_WEBHOOK_URL=https://YOUR_WEBHOOK_HOST/grok-facts
GROK_WEBHOOK_SECRET=YOUR_HMAC_SECRET
GROK_WEBHOOK_COOLDOWN_SECONDS=5
